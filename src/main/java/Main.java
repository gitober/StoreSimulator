import view.SimulatorGUI;

/**
 * Entry point for the simulation application.
 * Launches the JavaFX-based simulator graphical user interface.
 */
public class Main {

	/**
	 * Main method that starts the simulation application.
	 *
	 * @param args The command line arguments.
	 */
	public static void main(String[] args) {
		SimulatorGUI.main(args);
	}
}
